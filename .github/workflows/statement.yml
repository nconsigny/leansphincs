name: MVP statement and harness
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  statement:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      BENCHMARK_INSECURE_LOCAL: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
      - run: bash setup.sh
      - run: python3 -m unittest discover -s tests -v
      - run: lake build LeanSphincsTest.BoundExamples LeanSphincsTest.SigningFailure
      - run: lake build LeanSphincsTest.OTSScore LeanSphincsTest.OTSGraph LeanSphincsTest.OTSEvaluate
      - run: lake build LeanSphincsTest.OTSReconstruct LeanSphincsTest.OTSFailure
      - run: lake env lean scripts/check-ots-axioms.lean
      - run: python3 scripts/test-comparator.py
      - run: python3 scripts/test-verifier.py --insecure-local
